<?php
/**
 * {{class}} Index View
 *
 * ReflectiveTable auto-discovers columns from model $fillable,
 * bulk actions from HasDefaultBulkActions, and enables search/filters.
 */
use {{app_namespace}}\Models\{{class}};

mm_table({{class}}::class)
    // ->excludeColumns(['internal_field'])  // Hide specific columns
    // ->sortBy('created_at', 'DESC')        // Override default sort
    // ->withoutBulkActions()                // Disable bulk actions
    ->render();
