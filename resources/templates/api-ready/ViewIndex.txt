<?php
/**
 * {{class}} Index View - API Ready
 *
 * ReflectiveTable provides:
 * - Auto-discovered columns from model $fillable
 * - Bulk actions from HasDefaultBulkActions trait + #[BulkAction] methods
 * - Search across searchable fields
 * - Form filters for filtering data
 * - Auto-formatting for dates, booleans, foreign keys
 */
use {{app_namespace}}\Models\{{class}};
use MakermakerCore\Admin\ReflectiveTable;

// Zero-config version:
// mm_table({{class}}::class)->render();

// Customized version:
ReflectiveTable::for({{class}}::class)
    // Column control
    // ->excludeColumns(['metadata', 'internal_notes'])
    // ->columns(['id', 'title', 'status', 'created_at'])  // Explicit columns

    // Sorting
    // ->sortBy('created_at', 'DESC')

    // Custom column rendering
    // ->columnCallback('status', fn($val) => "<span class='badge badge-{$val}'>{$val}</span>")

    // Feature toggles
    // ->withoutBulkActions()
    // ->withoutSearchColumns()
    // ->withoutFormFilters()

    ->render();
